/**
 * Where a grant written from the Agents page's matrix comes back to (ACT-9):
 * the page it was pressed on, named by one fixed value so the form can never
 * redirect anywhere else.
 */
export const RETURN_FIELD = 'return_to';
export const RETURN_TO_AGENTS = 'agents';

/**
The Agents page, where the matrix of grants lives.
*/
export const AGENTS_PATH = '/account/agents';
