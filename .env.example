# Copy to .env for local development. Every variable is optional; the
# defaults bind to loopback. See docs/SPEC.md §10 for the full reference.
VAULTGATE_HOST=127.0.0.1
VAULTGATE_PORT=8080
VAULTGATE_LOG_LEVEL=info
